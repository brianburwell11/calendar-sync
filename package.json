{
  "name": "calendar-sync",
  "version": "0.1.0",
  "private": true,
  "description": "Mirror events from multiple Google org calendars into one main calendar (Google Apps Script + Sheet config).",
  "scripts": {
    "test": "node --test \"test/local/**/*.test.js\"",
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "logs": "clasp logs",
    "watch": "clasp push --watch"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
